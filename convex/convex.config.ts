import { defineConfig } from 'convex/server';

export default defineConfig({
  // Your Convex deployment URL will be set via environment variables
  // Run `npx convex dev` to connect to your backend
});
